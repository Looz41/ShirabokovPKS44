Задача 2:

```
Разработайте программу на C#, которая создает массив строк, запрашивает у пользователя новую строку и проверяет, содержится ли эта строка в массиве. 
Если строка найдена, программа выводит сообщение "Строка найдена!", в противном случае выводится сообщение "Строка не найдена!".
```
